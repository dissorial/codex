#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export CODEX_AGN_MODEL="${CODEX_BEDROCK_CLAUDE_MODEL:-global.anthropic.claude-opus-4-6-v1}"
exec "$repo_dir/codex-agn" "$@"
