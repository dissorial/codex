#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export CODEX_AGN_MODEL="${CODEX_GEMINI_MODEL:-gemini-3.1-pro-preview-customtools}"
exec "$repo_dir/codex-agn" "$@"
